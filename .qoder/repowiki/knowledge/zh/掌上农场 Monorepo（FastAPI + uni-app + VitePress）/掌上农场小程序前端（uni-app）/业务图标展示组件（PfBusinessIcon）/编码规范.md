- 图标名通过 TypeScript 字面量联合类型 BusinessIconName 显式枚举，新增图标需同步添加类型声明与同名 PNG 文件。
- 组件样式遵循 BEM 命名规范，尺寸、变体、状态分别以 --size、--variant、--muted 修饰类组合呈现。
- 视觉常量如颜色与圆角统一从 ../styles/design-tokens.scss 引入，避免在组件内硬编码设计值。
- 图片资源按语义化文件名存放于 static/icons/business/，并通过字符串模板动态拼接相对路径加载。