测试以 pytest 为运行框架，`conftest.py` 通过 `os.environ.setdefault` 注入 `DATABASE_URL`（指向本地 MySQL `pocket_farm`）和 `JWT_SECRET_KEY`，并定义 `autouse=True` 的 `clean_test_user` fixture：每个用例结束后按依赖顺序（HarvestRecord → FarmOperation → Production → Plot → Farm → User）清理手机号 `13800000001..6` 产生的数据并调用 `engine.dispose()` 释放连接。各 `test_*.py` 文件按领域划分（auth/farms/plots/productions/operations/harvests/phase9_workflows/health），每个模块内自建同构的 `request_api(path, method, json, headers)` 异步函数，通过 `httpx.AsyncClient(transport=ASGITransport(app=app))` 直接调用 `app.main.app` 而不启动真实 HTTP 服务；需要鉴权的用例通过登录 `/api/v1/auth/login` 获取 Bearer token 后在 `Authorization` 头中传递。`test_phase9_workflows.py` 作为跨模块端到端场景，封装 `login/call/create_farm/create_plot/start_production/record_operation/record_harvest/end_production` 等高层 helper，串联 farm→plot→production→operation→harvest→end 完整业务流程。