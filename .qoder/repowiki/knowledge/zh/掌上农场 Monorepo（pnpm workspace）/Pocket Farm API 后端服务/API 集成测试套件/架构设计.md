测试以 pytest + conftest.py 为入口：通过 `os.environ.setdefault` 注入 MySQL 连接串与 JWT 密钥，使用 `autouse=True` 的 `clean_test_user` fixture 在每个用例结束后按依赖顺序（HarvestRecord → FarmOperation → Production → Plot → Farm → User）清理由测试手机号 `13800000001..6` 产生的数据并释放引擎。各 `test_*.py` 文件按领域划分（auth/farms/plots/productions/operations/harvests/phase9_workflows/health），每个模块内自建 `request_api` 辅助函数，通过 `httpx.ASGITransport(app=app)` 直接调用 `app.main.app` 而不启动真实 HTTP 服务；需要鉴权的用例通过登录 `/api/v1/auth/login` 获取 Bearer token 后在 `Authorization` 头中传递。`test_phase9_workflows.py` 串联 farm→plot→production→operation→harvest→end 的完整业务流程，作为跨模块的端到端场景。